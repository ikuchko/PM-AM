<h1>$user.getName()</h1>
<br>
<hr>
#foreach ($epic in $epics)
  <div class="row">
  <h1>$epic.getTitle()</h1>
  #foreach ($story in $epic.getAllSubTasks())
  <div class="col-md-4">
     <h2>$story.getTitle()</h2>
    #foreach ($task in $story.getAllSubTasks(1))
    <form action="/assign-inprogress" method="post" class="form-inline" role="form">
      <div class="form-group">
      <h4>$task.getTitle()</h4></div><button type="submit" class="btn btn-success btn-block small">+</button></form>

    #end
  </div>
  #end
</div>
#end
